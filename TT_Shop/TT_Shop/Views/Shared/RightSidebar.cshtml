<div class="right-sidebar" style="height: 100vh; border: 1px solid #ccc; background-color: #f8f9fa;">
    <h2 class="text-center">
        <a href="@Url.Action("Detail", "Home", new { id = 3 })">
            <img src="~/images/QC/QCSS21Ul.jpg" class="img-fluid custom-img" />
        </a>
        <a href="@Url.Action("Detail", "Home", new { id = 2 })">
            <img src="~/images/QC/QCIP14Pro.jpeg" class="img-fluid custom-img" />
        </a>
        <a href="@Url.Action("Detail", "Home", new { id = 11 })">
            <img src="~/images/QC/QCQC45.png" class="img-fluid custom-img" />
        </a>
    </h2>
</div>

<style>
    .custom-img {
        width: 100%; /* Chiều rộng linh hoạt theo phần chứa */
        height: auto; /* Giữ tỷ lệ ảnh */
        max-width: 100%; /* Đảm bảo ảnh không vượt quá chiều rộng của phần chứa */
        margin: 10px 0; /* Khoảng cách giữa các ảnh */
        border: 2px solid #007bff; /* Viền ảnh */
        border-radius: 8px; /* Bo tròn góc ảnh */
        object-fit: contain; /* Giữ tỷ lệ ảnh mà không bị cắt bớt */
    }

    /* Đảm bảo rằng ảnh trong phần này không bị vỡ hoặc bị biến dạng */
    .right-sidebar img {
        display: block;
        margin: 10px auto; /* Thêm khoảng cách giữa các ảnh */
        width: 100%; /* Làm cho ảnh co giãn theo chiều rộng của phần chứa */
        max-width: 100%; /* Đảm bảo ảnh không vượt quá chiều rộng của phần chứa */
    }
</style>
